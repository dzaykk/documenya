services:
  api:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/app

  worker:
    command: celery -A app.workers.celery_app:celery_app worker --pool=solo --loglevel=info
    volumes:
      - .:/app